## ADDED Requirements

### Requirement: Published page readability
The system SHALL render the published page as a minimal destination with strong typography and no editor chrome.

#### Scenario: Open published page
- **WHEN** a visitor opens a published URL
- **THEN** they see the message as the primary content on the page
- **AND** the page uses the chosen theme tokens for background, color, alignment, and scale.
